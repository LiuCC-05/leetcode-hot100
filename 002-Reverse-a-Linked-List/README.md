# 002. 反向链表
### 📝 题目描述
给你单链表的头节点 head ，请你反转链表，并返回反转后的链表。

### 🔑 关键知识
1. **定义参数**：pre，curr，nxt;
2. **初始化**:pre =None,curr=head;
   
### 🛠️ 避坑小结
1. **顺序不能乱**：先存 `nxt`，再改 `curr.next`。
2. **返回值是 prev**：当 `curr` 跑过头变成 `None` 时，`prev` 刚好站在新链表的最高点。
3. **断后很重要**：第一个节点的 `.next` 必须指向 `None`，否则链表会变成一个无限旋转的环！
   
