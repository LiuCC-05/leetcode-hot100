# 234. 回文链表 (Palindrome Linked List) 🔑

### 📝 题目概述
判断一个单链表是否为回文结构。  
*   **示例**：`1 -> 2 -> 2 -> 1` 是回文；`1 -> 2` 不是。
*   **性能要求**：时间复杂度 **$O(n)$**，空间复杂度 **$O(1)$**。

---

### 🚀 核心策略：进阶“三步走”方案
为了实现 **$O(1)$** 空间复杂度，我们不使用额外的数组存值，而是直接在原链表上进行操作。

| 步骤 | 核心动作 | 技术点 | 目的 |
|---|---|---|---|
| **1. 寻找中点** | 快指针走两步，慢指针走一步。 | **快慢指针** | 定位后半部分的起始位置。 |
| **2. 原地反转** | 将后半部分链表的指针方向彻底调转。 | **反转链表** | 方便从后往前与前半部分对比。 |
| **3. 双头比对** | 前后两段同时开始遍历并比较数值。 | **双指针比对** | 验证对应位置的值是否一致。 |

---

### 🎨 逻辑深度拆解

#### 1. 快慢指针 (Find Middle)
*   `fast` 指针速度是 `slow` 的两倍。
*   当 `fast` 抵达终点时，`slow` 恰好位于链表的中点位置。

#### 2. 原地反转 (Reverse Second Half)
对 `slow` 之后的节点执行反转舞步。  
*   **初始**：`1 -> 2 -> 2 -> 1`
*   **反转后**：前半段 `1 -> 2`，后半段 `1 -> 2` (指向发生改变)。

#### 3. 最后的审判 (Compare)
*   `p1` 指向原链表头，`p2` 指向反转后的后半段头部。
*   同步移动，对比 `val`。若全部相等，则为回文。

---

### 🐍 Python 代码实现 ($O(1)$ 空间)

```python
class Solution(object):
    def isPalindrome(self, head):
        if not head or not head.next:
            return True

        # --- 第一步：快慢指针找中点 ---
        fast = slow = head
        while fast and fast.next:
            fast = fast.next.next
            slow = slow.next

        # --- 第二步：原地反转后半部分 ---
        prev = None
        curr = slow
        while curr:
            nxt = curr.next
            curr.next = prev
            prev = curr
            curr = nxt
        
        # --- 第三步：前后对比 ---
        p1, p2 = head, prev 
        while p2: # 只需要比完后半段即可
            if p1.val != p2.val:
                return False
            p1 = p1.next
            p2 = p2.next
        
        return True
